<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Vehicletrackermobile"
            android:name=".VehicleTrackerApplication"
            android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".presentation.authentication.AuthenticationActivity"
            android:exported="true"
            android:launchMode="singleTop"
        >
        </activity>
        <activity
            android:name=".presentation.authentication.RedirectActivity"
            android:exported="true"
        >

            <intent-filter>

                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data
                    android:scheme="${redirectScheme}"
                    android:host="${redirectHost}"
                    android:path="${redirectPath}"
                />

            </intent-filter>

        </activity>
        <activity
                android:name=".presentation.welcome.WelcomeActivity"
                android:exported="true"
                android:theme="@style/Theme.Vehicletrackermobile"
        >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
                android:name=".presentation.home.HomeActivity"
                android:exported="false"
                android:theme="@style/Theme.Vehicletrackermobile"
        >
        </activity>
        <activity
                android:name=".presentation.vehicles.VehiclesActivity"
                android:exported="false"
                android:theme="@style/Theme.Vehicletrackermobile"
        >
        </activity>
        <activity
                android:name=".presentation.devices.DevicesActivity"
                android:exported="false"
                android:theme="@style/Theme.Vehicletrackermobile"
        >
        </activity>
        <activity
                android:name=".presentation.devices.CreateDeviceActivity"
                android:exported="false"
                android:theme="@style/Theme.Vehicletrackermobile"
        >
        </activity>
        <activity
                android:name=".presentation.devices.DeviceConfigurationActivity"
                android:exported="false"
                android:theme="@style/Theme.Vehicletrackermobile"
        >
        </activity>
    </application>

</manifest>